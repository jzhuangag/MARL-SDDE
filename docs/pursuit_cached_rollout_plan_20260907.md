## Material Passport

- Origin Skill: academic-research-suite
- Origin Mode: outcome-free cached-behavior qualification plan
- Origin Date: 2026-09-07
- Verification Status: FROZEN STRUCTURAL AUDIT; NO REWARD OUTCOME
- Version Label: pursuit_cached_rollout_qualification_v1

# Pursuit owner-cache rollout qualification

This audit closes the behavior link not covered by the cache-array identity
test. On each real PettingZoo Pursuit step, one asynchronous owner worker forms
the complete joint action from its current self policy and recipient-specific
cached teammate policies. A selected local factor refresh must change only that
owner's donor cache, charge the actor payload, and make the realized donor
action equal the current donor actor's action before the environment step.

The run uses untouched seeds `92200--92207`, eight pursuers, thirty evaders,
forty cycles, observation range seven, and the public degree-four factor graph.
It reads no reward value and trains no policy.

Frozen gates require: every environment step is driven by a complete cached
profile; non-null refreshes and positive byte charges occur; every refreshed
donor action equals its current actor action; at least one refresh changes the
realized cached action; and dynamic graph turnover occurs. Primary and isolated
reproduction JSON must be byte-identical.

Frozen SHA-256 values:

- cached rollout helper: `b69c676ff83e1e213372abc9a360cf0de920207445e5a6fcf3ccb5435599a424`;
- audit runner: `5d1e42ffe638a4529e2ce6edfa3e78c666cf23248ef5c4c0494a964fb5226f42`;
- helper tests: `d0cfa94f6dbddb9abe03cc35290a928a152be69b9e9ac5cc128e04ab2f08bc6a`;
- audit smoke test: `4d4a279d16d953063232af4a9bd993a47b8fbc382f62c10ff75e2be7ddbab6eb`.

Passing remains a structural qualification only. It does not establish critic
calibration, return improvement, or GPU authorization.
