# T-023 final decision

Decision: **permit outcome-free EXP-018B formal preregistration design; do not
yet run it.**

EXP-018A remains failed and cannot provide formal evidence. T-023 establishes
only that a 192-seed formal calibration study has a prospective uncertainty
margin compatible with the already registered 0.20/0.50 aggregate tolerances.

The new design must correct three protocol defects rather than loosen a failed
threshold:

1. reuse exactly one q=1 CRN baseline across rho;
2. use equivalence-style aggregate calibration endpoints instead of demanding
   strict ordering for theoretical differences below 5%;
3. exclude absolute paths from scientific summaries.

All formal code, hashes, new seeds, two-endpoint FWER rule, and failure action
must be frozen in an independent commit before any new Markov transition is
generated. GPU and HPC4 remain unauthorized.
