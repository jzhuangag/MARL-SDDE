## Material Passport

- Origin Skill: academic-research-suite
- Origin Mode: independent CPU confirmation audit
- Origin Date: 2026-09-07
- Verification Status: ALL 12 FROZEN GATES PASS; BYTE-EXACT REPRODUCTION
- Version Label: PDSG-FR-001-validation-v1

# PDSG-FR-001 independent confirmation

## Decision

The frozen Markov forecast-reversal confirmation passes all twelve gates. It
supports the topology-robust core Lyapunov mechanism and authorizes the next
outcome-free Pursuit cache/critic interface qualification. It does not authorize
a GPU efficacy run, a persistent-cache-energy claim, or a standard-MARL claim.

The preregistration is commit `4c69c03`. The configuration and runner hashes
match the frozen values. Development seeds `97100--97107` are excluded; the
confirmation uses the untouched seeds `98100--98131`.

## Primary result

The run contains all `24 * 32 * 10 = 7,680` endpoint identities. In the twelve
preregistered favorable high-cycle cells, the plug-in core controller has
geometric cumulative-risk ratio `0.5878309403` relative to the strong online
state-myopic scheduler, a `41.2169%` reduction. It improves all `12/12` cells
and all `384/384` paired seed-cells. Median exact-oracle headroom recovery is
`0.8756521826`, and all twelve favorable points pass the registered
return--message Pareto test.

Across all 24 cells, its geometric ratio is `0.7492747498`, a `25.0725%`
reduction. The low-cycle ratio is `1.0067548482`; the resulting phase gap is
`0.4189239080`. This is the registered evidence that finite-horizon Markov
forecasting matters in the favorable phase, not a universal no-harm claim.

All environment-transition counts match within each scenario/seed and no
communication-budget violation occurs.

## Gate ledger

| Gate | Result |
|---|---:|
| P1 complete identities | pass |
| P2 finite and equal environment transitions | pass |
| P3 communication budget | pass |
| P4 favorable ratio at most 0.75 | pass: 0.587831 |
| P5 all favorable cells improve | pass: 12/12 |
| P6 favorable seedwise win fraction at least 0.90 | pass: 1.000 |
| P7 median oracle recovery at least 0.75 | pass: 0.875652 |
| P8 favorable Pareto points | pass: 12/12 |
| P9 phase separation at least 0.25 | pass: 0.418924 |
| P10 all-cell ratio at most 0.85 | pass: 0.749275 |
| P11 oracle ratio at most 0.80 | pass: 0.669906 |
| P12 isolated byte-exact reproduction | pass |

## Reproduction and provenance

Primary and isolated reproduction endpoints have the identical SHA-256
`82e20f5d6c3b62510050ed75779a4d18ada007d0e14fa19288e4da392fda1751`.
Their summaries have the identical SHA-256
`702443e781014fc07ff2fb6adbd6525853e09ca7ff774ad6a957420b1321fc9e`.
The reproduction was assembled deterministically from four isolated seed
chunks; no scientific row was rerun or altered during validation.

The result directories remain ignored under
`experiments/policy_dependency_sync/results/`. The machine-readable committed
summary is `docs/pdsg_fr001_confirmation_summary_20260907.json`.

## Claim boundary after the two-layer theorem correction

The executed `plugin_joint` index contains predicted future alignment,
action-specific packet curvature, receipt-motion penalty, and the virtual
communication queue. Its cache reset, outgoing-cache linear term, and
outgoing-cache curvature are all zero. Consequently this result confirms the
Layer-I controller based on `VF + Q^2/(2 nu)`.

It does not test Layer II. In particular, it neither copies a persistent
teammate actor into a worker cache nor charges a refresh that persists when the
receipt-time packet weight is zero. Those semantics require separate Pursuit
interface invariants before any efficacy experiment.
