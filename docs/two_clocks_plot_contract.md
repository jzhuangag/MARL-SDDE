# Two Clocks plot contract

This remote `plots` directory is reserved for figures produced from a frozen
scientific pilot or formal analyzer. The outcome-free G0 gate must not create a
performance plot. Any later plotting command must record the source result
manifest, analyzer commit, configuration hash, and output SHA-256 next to the
figure. Development and formal outputs must remain visibly separated.
