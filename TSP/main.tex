\documentclass[journal]{IEEEtran}
\pdfinfoomitdate=1
\pdftrailerid{}
\pdfsuppressptexinfo=-1
\usepackage{amsmath,amssymb,amsthm,bm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{url}
\usepackage{algorithmic}
\usepackage[hidelinks]{hyperref}
\newtheorem{assumption}{Assumption}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Pp}{\mathbb{P}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\A}{\mathcal{A}}
\newcommand{\F}{\mathcal{F}}
\newcommand{\KL}{\operatorname{KL}}
\newcommand{\TV}{\operatorname{TV}}
\newcommand{\tr}{\operatorname{tr}}
\title{Learning-Aware Lyapunov Resource Control for Correlated Multi-Agent Markov Learning}
\author{Anonymous Authors}
\begin{document}
\maketitle
\begin{abstract}
Parallel Markov trajectories can accelerate policy evaluation, but their value depends on cross-agent correlation, temporal persistence, communication, and update staleness.
We formulate their allocation as finite-budget learning-value control in which a server selects participation, physical sample spacing, and step size before observing the next update.
A discrete-time Lyapunov certificate converts these decisions into a terminal-risk objective for affine delayed temporal-difference learning.
Its terms retain cross-agent second moments, finite-mixing bias, and the delay profile, so the certificate both proves convergence and designs the resource action.
When dependence is unknown, a learning-aware gate purchases a fully charged likelihood probe only when its downstream risk reduction exceeds message, transition, delay, and decision-error costs.
We establish a stopped adaptive change-of-measure lower bound, a finite-horizon performance and safety theorem, and a necessary--sufficient budget-threshold sandwich on a separated Markov class.
Registered CPU studies recover correlation-limited speedup, calibrate the finite-time bounds, and reduce two finite-budget risks by 54.4 and 10.9 percent.
On 64 held-out seeds, joint control reduces parameter-error and discounted-return-estimation AUC by 9.9 and 11.3 percent against a frozen strong fixed baseline.
A preregistered 48-run multi-agent proximal policy optimization (MAPPO) study selects dependence-matched participation in all 16 runs and improves equal-mixture team-return AUC by 2.32 percent over fixed broad participation under identical budgets.
\end{abstract}
\begin{IEEEkeywords}
Multi-agent learning, Markov data, temporal-difference learning, adaptive sensing, Lyapunov methods.
\end{IEEEkeywords}

\section{Introduction}
Distributed policy evaluation is a statistical core of large-scale reinforcement learning.
The environment state records the physical configuration that determines the next reward and transition, a target policy maps each state to an action distribution, and its value is the expected discounted return generated by repeated execution.
In parallel simulation, federated evaluation, and centralized training, multiple actors run the target policy on Markovian environment copies and send temporal-difference observations to a server that maintains a common value parameter.
The design question is how the server should allocate these spatial and temporal observations when communication and environment interaction are both limited.

Parallel trajectories are valuable only to the extent that they carry distinct learning information.
Common innovations can couple actors while leaving every marginal transition law unchanged.
For cross-agent correlation $\rho$, averaging $q$ Gaussian observations yields effective participation $q/[1+(q-1)\rho]$, which saturates at $1/\rho$ rather than growing linearly.
Because each participant consumes a message and may return a stale update, broad participation can reduce the affordable contractions while contributing little new information.

Temporal sampling and adaptation introduce a second coupling.
Advancing a Markov chain by $b$ physical transitions before retaining a sample reduces conditional mixing bias but consumes the environment-clock budget, while a larger step size $\eta$ accelerates transient contraction but raises the residual noise floor and can destabilize a delayed recursion.
If the dependence regime is unknown, estimating it consumes the same messages and transitions that would otherwise be used for learning.
The relevant objective is consequently the terminal or trajectory-integrated learning risk under separate message and environment-clock budgets, not single-update variance, asymptotic error, or wall-clock time alone.

Finite-time temporal-difference and Markovian stochastic-approximation analyses characterize mixing, step-size, and delay effects for a prescribed observation process, but do not choose how many correlated trajectories to aggregate or how far to advance them before sampling.
Distributed and federated reinforcement-learning methods establish parallel speedups and sample--communication tradeoffs, while client-selection and feedback-control methods regulate participation.
Their participation mechanisms do not jointly price cross-agent dependence, physical Markov spacing, stable adaptation, and the resulting number of remaining learning updates.

Delay-aware stochastic optimization provides a complementary perspective.
Recent stochastic delay differential equation (SDDE) analysis optimizes worker activation and staleness policies for asynchronous stochastic gradient descent, and delay-adaptive stochastic approximation improves rates under Markovian sampling~\cite{yu2025sdde,adibi2024delayed,dalfabbro2024dasa}.
These methods prescribe the data dependence and optimize delay or runtime behavior; they do not value correlated Markov observations under a downstream learning budget.
Sequential experimental design can identify an unknown dependence instance, but its information objective does not determine whether the remaining learner can amortize the sensing cost.
The unresolved problem is therefore to value spatial sampling, temporal decorrelation, stable adaptation, and dependence identification through one finite-budget learning objective.

We address this problem through learning-aware Lyapunov resource control.
Before each learning block, the server selects
\begin{equation}
a=(q,b,\eta),
\label{eq:resource-action-intro}
\end{equation}
where $q$ is the integer participation, $b$ is the physical Markov spacing, and $\eta$ is the step size.
An exact discrete-time Lyapunov drift bound maps every action to an action-dependent contraction coefficient and a correlation-, mixing-, and delay-dependent residual floor.
Composing that drift over the action-dependent number of affordable updates produces a certified terminal-risk score.
The controller minimizes this score over the public mixed discrete--continuous action domain: it solves a bounded scalar problem in $\eta$ for every physically discrete $(q,b)$ pair and then takes the exact catalogue minimizer.
The Lyapunov function therefore designs the resource action as well as proving its convergence, and the computation requires no matrix inversion or preconditioning.

Unknown dependence introduces a second control decision: whether to learn which certified action should be used.
The controller admits a likelihood probe only when the certified downstream gain of the instance-specific action exceeds the probe's complete message, transition, delay, and decision-error cost.
This yields two distinct boundaries.
Below an information boundary, reliable correlation identification is impossible.
Between the information and learning-value boundaries, identification is possible but its downstream gain cannot amortize its resource cost.
Above the learning-value boundary, the controller probes and commits while satisfying an explicit low-instance risk allowance.
This phase separation joins stochastic optimization and controlled sensing through the same terminal learning objective.

The main contributions of this paper are summarized as follows.
\begin{itemize}
\item \textbf{Finite-Budget Learning-Value Formulation:} We formulate correlated multi-agent policy evaluation as joint control of spatial participation, physical Markov spacing, and step size under separate message and environment-clock budgets.
An exact Gaussian subclass exposes the opposing variance, mixing, and horizon effects and gives a correlation-limited speedup ceiling.
\item \textbf{Lyapunov-Certified Joint Resource Control:} We derive a finite-time Lyapunov certificate for affine delayed temporal-difference recursions with conditional Markov bias and dependent random Jacobian--innovation pairs.
The certificate supplies both the convergence theorem and an implementable joint action score, with scalar step search and no matrix inversion or preconditioning.
Its moving-policy extension quantifies how critic contraction competes with policy-induced target motion and transfers the tracking bound to advantage error.
\item \textbf{Information-to-Learning Phase Characterization:} We characterize the price of learning dependence.
A stopped adaptive change-of-measure identity and a controlled-belief occupation program give a necessary information cost, while a learning-aware gate gives a sufficient finite-budget performance and safety guarantee.
On a compact separated class, the two thresholds match up to class constants and explicit oracle and safety scales.
\item \textbf{Fully Charged Mechanism and Policy-Learning Evidence:} We validate the complete causal chain with fully charged experiments.
Controlled studies recover the predicted participation phase, verify the finite-time confidence bounds, demonstrate 54.4 and 10.9 percent learning-value improvements, and reduce held-out parameter-error and discounted-return-estimation AUC by 9.9 and 11.3 percent over a development-selected strong fixed-participation baseline.
A separately preregistered multi-agent proximal policy optimization study selects broad participation for independent rollouts and local participation for shared rollouts in all 16 held-out controller runs, producing 5.00 percent shared-regime and 2.32 percent equal-mixture team-return AUC gains over fixed broad participation.
\end{itemize}

The remainder of the paper develops this argument as follows.
Section~II positions the work relative to stochastic approximation, distributed reinforcement learning, delay control, and adaptive sensing.
Section~III defines the Markov policy-evaluation model, budgets, and risks.
Section~IV develops the correlation and resource geometry.
Section~V derives the Lyapunov controller.
Section~VI establishes the learning-aware adaptation theory.
Section~VII describes implementation and the controlled-SDDE interpretation.
Section~VIII presents the experiments, and Section~IX concludes the paper.

\section{Related Work}
Temporal-difference learning originates in stochastic approximation and fixed-point estimation~\cite{sutton1988td,tsitsiklis1997td,robbins1951stochastic,polyak1992averaging}.
Modern finite-time analyses quantify the interaction among constant steps, Markov mixing, and value-function error~\cite{bhandari2018finite,srikant2019finite,mou2022optimal,paulin2015concentration}; Lyapunov bounds have also motivated adaptive learning-rate rules in two-time-scale reinforcement learning~\cite{gupta2019adaptive}.
Our distinction is that the finite-time bound is the optimization criterion for the complete resource action $(q,b,\eta)$, rather than an analysis of a prescribed sampler or a trigger for changing only the step.

Distributed and federated reinforcement learning study consensus, parallel sampling, heterogeneous coverage, and sample--communication tradeoffs~\cite{doan2019distributed,zhang2018decentralized,khodadadian2022federated,woo2023heterogeneity,salgia2024tradeoff,lan2023communication}.
Related client-selection and adaptive-batch methods reduce sampling variance or regulate participation, including feedback control of participation rates and Lyapunov scheduling of devices and transmit power~\cite{fraboni2021clustered,de2017adaptive,cummins2025feedback,perazzone2025lyapunov,ren2019antithetic}.
These methods target participation dynamics, update variance, or communication time; they do not couple cross-agent covariance with physical Markov spacing and the finite number of post-decision updates.

Delayed stochastic optimization and stochastic approximation quantify stale-update effects and design delay-adaptive rules~\cite{agarwal2011distributed,lian2015asynchronous,arjevani2020delayed,adibi2024delayed,dalfabbro2024dasa}.
Most closely, Yu, Chen, and Poor use an SDDE and arrival-process approximation to optimize activated workers and staleness thresholds for asynchronous stochastic gradient descent~\cite{yu2025sdde}.
Our controlled SDDE instead exposes the geometry of correlated Markov observations, while the exact discrete recursion prices delay inside a terminal-risk objective that jointly chooses participation, spacing, and step size.

Sequential experimental design, controlled sensing, and best-arm identification provide change-of-measure and allocation tools~\cite{chernoff1959sequential,nitinawarat2015controlled,kaufmann2016complexity,garivier2016optimal,moulos2019markovian,saad2023covariance,vannella2023multiagent}.
Those tools price statistical identification, whereas our admission test also prices the downstream risk of the learner that remains after probing.
This distinction produces an identifiable-but-not-yet-valuable phase.
Conservative bandits compare an adaptive policy with a baseline through a reward budget~\cite{wu2016conservative}; our safety slack instead follows from the same terminal-risk certificate and decomposes probe consumption, delay consumption, correct-commit gain, and wrong-decision loss.

\section{Model and Finite-Budget Objective}
\subsection{Fixed-policy evaluation with multiple actors}
Let $({\cal S},{\cal U},P,r,\gamma)$ be a discounted Markov decision process and let $\pi_{\rm tar}(u\mid s)$ be a fixed target policy.
Under $\pi_{\rm tar}$, the state--action process is a Markov reward process with transition kernel $P_{\pi}$ and stationary distribution $\pi$.
For a feature vector $\phi(s)\in\R^d$, the linear value approximation is $V_w(s)=\phi(s)^\top w$.
Its projected Bellman fixed point $w^\star$ solves
\begin{equation}
A w^\star=c,
\quad
A=\E_\pi[\phi_t(\phi_t-\gamma\phi_{t+1})^\top],
\quad
c=\E_\pi[r_t\phi_t].
\label{eq:projected-fixed-point}
\end{equation}
The target of learning is common across actors; their Markov paths may nevertheless be cross-correlated.

Let $S_k$ denote the physical observation clock at retained learning decision $k$.
The server chooses $q_k\in\{1,\ldots,Q\}$ actors and spacing $b_k\in\mathbb{N}$, so $S_k-S_{k-1}=b_k$.
Actor $i$ returns $X_{i,k}=(s_{i,S_k},r_{i,S_k},s_{i,S_k+1})$ and the corresponding temporal-difference tuple.
The participating joint chain has invariant distribution $\pi_q$ and the total-variation certificate
\begin{equation}
\sup_z\left\|P_q^b(z,\cdot)-\pi_q\right\|_{\rm TV}\le C_qr_q^b,
\quad 0<r_q<1.
\label{eq:mixing}
\end{equation}
The marginal law of every actor is the target-policy law, whereas cross-agent dependence appears in the stationary moments of their aggregate update.
This is not a partially observed decision problem: the controller does not infer an execution-time environment state or change $\pi_{\rm tar}$.
Its uncertainty concerns the dependence and finite-time risk of the training data.

For the identification layer, a Gaussian common-factor subclass makes the dependence parameter explicit.
Under instance $j\in\{0,1\}$,
\begin{align}
C_{S_k}&=\lambda^{b_k}C_{S_{k-1}}+\sqrt{1-\lambda^{2b_k}}\,\xi_{j,k},\nonumber\\
X_{i,k}&=C_{S_k}+\epsilon_{i,k},
\label{eq:common-factor}
\end{align}
where $\xi_{j,k}\sim\mathcal{N}(0,\theta_j)$, $\epsilon_{i,k}\sim\mathcal{N}(0,1)$, and all private innovations are independent.
The common process is initialized with $C_{S_0}\sim\mathcal N(0,\theta_j)$, so the probe law is stationary.
The corresponding same-time correlation is $\rho_j=\theta_j/(1+\theta_j)$.
The model preserves every individual marginal while exposing effective-sample-size saturation caused by common randomness.
It is used to prove the information boundary; the convergence theorem below applies to bounded affine temporal-difference observations satisfying Assumption~\ref{ass:catalogue}.

\subsection{Affine delayed temporal-difference recursion}
Let $w^\star\in\R^d$ be the projected temporal-difference fixed point and $e_k=w_k-w^\star$.
For action $a=(q,b,\eta)$ and delays $0\le\tau_i\le D$, the retained recursion is
\begin{equation}
e_{k+1}=e_k-\frac{\eta}{q}\sum_{i=1}^{q}\left(H_{i,k}e_{k-\tau_i}-\xi_{i,k}\right).
\label{eq:affine-td}
\end{equation}
The rank-one matrix $H_{i,k}$ is the sample temporal-difference Jacobian and $\xi_{i,k}$ is the affine innovation at $w^\star$.
For linear TD(0), these quantities are
\begin{equation}
H_{i,k}=\phi_{i,k}(\phi_{i,k}-\gamma\phi'_{i,k})^\top,
\quad
\xi_{i,k}=\phi_{i,k}\delta_i(w^\star),
\label{eq:td-components}
\end{equation}
where $\delta_i(w)=r_{i,k}+\gamma\phi_{i,k}'{}^\top w-\phi_{i,k}^\top w$.
The physical chain advances by $b$ transitions before each retained update, which makes the mixing error in \eqref{eq:mixing} a controlled quantity.

\begin{assumption}[Certified action domain]
The public sets ${\cal Q}\subseteq\{1,\ldots,Q\}$ and ${\cal B}\subset\mathbb N$ are finite, while the step interval $[\eta_{\min},\eta_{\max}]$ is compact.
For each $a=(q,b,\eta)$ in their product: (a) $\|H_{i,k}\|\le L$ and $\|\xi_{i,k}\|\le G$ almost surely; (b) the stationary mean matrix $A=\E_{\pi_q}H_{i,k}$ is common across $q$ and has symmetric part at least $\mu I$; (c) $0\le\tau_i\le D$ and the delay profile is known when the score is evaluated; and (d) the mixing and aggregate-moment upper certificates are predictable.
\label{ass:catalogue}
\end{assumption}

We write $\A={\cal Q}\times{\cal B}\times[\eta_{\min},\eta_{\max}]$ for this mixed action domain.
The finite sets for $q$ and $b$ encode implementable actor counts and admissible environment clocks rather than a continuous relaxation of a discrete system.
The controller optimizes $\eta$ continuously inside each discrete pair and therefore solves the stated mixed discrete--continuous problem exactly up to a chosen scalar-search tolerance.

\subsection{Dual budgets and terminal risk}
One retained update with $q$ actors costs $h+q$ message units and $b$ units of the parallel environment clock.
A transition taken simultaneously by several actors advances this clock once; the separate message budget charges every participating actor.
A feasible trajectory therefore satisfies
\begin{equation}
\sum_k(h+q_k)\le B_{\rm msg},
\quad
\sum_k b_k+D\mathbf{1}\{K>0\}\le B_{\rm env}.
\label{eq:budgets}
\end{equation}
For a stationary action $a$, let $N_a(B,D)$ be the number of updates usable after charging both budgets and the terminal delay.
Explicitly,
\begin{equation}
N_a(B,D)=\left[\min\left\{\left\lfloor\frac{B_{\rm msg}}{h+q}\right\rfloor,
\left\lfloor\frac{B_{\rm env}-D}{b}\right\rfloor\right\}\right]_+.
\label{eq:usable-horizon}
\end{equation}
The controller minimizes a finite-time certificate $\overline R_j(a;B,D)$ satisfying $\E_j\|e_{N_a}\|^2\le\overline R_j(a;B,D)$.
The primary risk is parameter mean-square error.
It also controls fixed-policy return estimation because, for a registered initial distribution $\nu$,
\begin{equation}
\E\left|J_\nu(w)-J_\nu(w^\star)\right|^2
\le \left\|\E_{s\sim\nu}\phi(s)\right\|^2\E\|e\|^2,
\label{eq:return-transfer}
\end{equation}
where $J_\nu(w)=\E_{s\sim\nu}[V_w(s)]$.

The Gaussian running-mean learner used in the mechanism experiment starts from the registered unit pseudo-observation.
For $r=\lambda^b$, its exact risk is
\begin{equation}
R_j^{\rm mean}(a,N)=
\frac{1+N/q+\theta_j\left[N+2\sum_{s=1}^{N-1}(N-s)r^s\right]}{(N+1)^2}.
\label{eq:mean-risk}
\end{equation}
Equation \eqref{eq:mean-risk} makes the two opposing effects transparent: increasing $q$ reduces the private-noise term $N/q$ but can reduce $N$, while increasing $b$ reduces $r$ but consumes environment transitions.

\section{Correlation and Resource Geometry}
\subsection{A sharp spatial speedup ceiling}
The statistical role of participation is exact in a Gaussian location subclass.
Let $Y_{i,t}=\vartheta+C_t+\epsilon_{i,t}$ with common variance $\sigma_c^2$, private variance $\sigma_e^2$, and independent rounds.
For a fixed participation $q$, the minimax squared-error risk after $T$ rounds is
\begin{equation}
R_q(T)=\frac{1}{T}\left(\sigma_c^2+\frac{\sigma_e^2}{q}\right).
\label{eq:location-risk}
\end{equation}
Writing $\rho=\sigma_c^2/(\sigma_c^2+\sigma_e^2)$ gives the speedup relative to one actor,
\begin{equation}
S(q,\rho)=\frac{R_1(T)}{R_q(T)}
=\frac{q}{1+(q-1)\rho}
\le\min\{q,\rho^{-1}\}.
\label{eq:speedup}
\end{equation}
Thus spatial scaling is linear only near independence.
For every fixed $\rho>0$, its benefit saturates while the message cost continues to grow linearly in $q$.

\begin{proposition}[Participation phase under a message budget]
Suppose the environment budget does not bind, the update overhead is $h$, and a total message budget $B_{\rm msg}$ is used with a fixed $q$.
Ignoring integer rounding, the minimax risk is proportional to
\begin{equation}
F(q;\rho,h)=(h+q)\left(\rho+\frac{1-\rho}{q}\right).
\label{eq:participation-phase-objective}
\end{equation}
Its continuous minimizer is
\begin{equation}
q_{\rm cont}^\star=\sqrt{\frac{h(1-\rho)}{\rho}},
\label{eq:participation-phase}
\end{equation}
clipped to $[1,Q]$, and an integer optimum is one of its two neighboring feasible participation levels.
\label{prop:q-phase}
\end{proposition}

Proposition~\ref{prop:q-phase} gives a nonheuristic phase boundary.
More fixed overhead favors larger batches, whereas stronger common randomness favors smaller participation.
The full controller extends this balance to a binding environment clock, Markov spacing, step size, and delay.

\subsection{Temporal spacing and the usable horizon}
For the common-factor process in \eqref{eq:common-factor}, retaining every $b$th observation changes persistence from $\lambda$ to $r=\lambda^b$.
The integrated correlation term in \eqref{eq:mean-risk} admits the closed form
\begin{equation}
\sum_{s=1}^{N-1}(N-s)r^s
=\frac{r\{(N-1)-Nr+r^N\}}{(1-r)^2}.
\label{eq:geometric-correlation-sum}
\end{equation}
Increasing $b$ decreases this term exponentially, but \eqref{eq:usable-horizon} decreases approximately as $1/b$ when the environment clock binds.
Consequently, aggressive thinning can make retained observations nearly independent and still increase terminal error.

For a fixed action, the message-limited and environment-limited horizons are
\begin{equation}
N^{\rm msg}_a=\left\lfloor\frac{B_{\rm msg}}{h+q}\right\rfloor,
\quad
N^{\rm env}_a=\left\lfloor\frac{B_{\rm env}-D}{b}\right\rfloor.
\label{eq:two-horizons}
\end{equation}
The active resource is the smaller horizon.
This minimum is why optimizing an effective sample size per update, a variance per message, or a mixing time alone can select the wrong action.

\subsection{From local drift to finite-budget value}
Let a candidate action have one-block contraction $c(a)<1$, residual floor $R_\star(a)$, and usable horizon $N_a$.
Its certified finite-budget value has the generic form
\begin{equation}
R_\star(a)+c(a)^{N_a/m}\bigl(R_0-R_\star(a)\bigr)_+.
\label{eq:generic-finite-budget-value}
\end{equation}
The first term favors low-variance, well-mixed updates, while the second favors rapid contraction and many affordable updates.
The next section derives $c(a)$ and $R_\star(a)$ for delayed affine temporal-difference learning rather than postulating this tradeoff.

\section{Lyapunov-Certified Joint Resource Control}
\subsection{Action-dependent one-block drift}
The base Lyapunov function is the squared parameter error $V(e)=\|e\|^2$.
Delay prevents its one-step drift from closing in $e_k$ alone because the update also contains $e_{k-\tau_i}$.
We therefore analyze the observable-independent sliding envelope
\begin{equation}
R_k=\max_{k-2D\le s\le k}\E V(e_s),
\label{eq:sliding-envelope}
\end{equation}
which is a Lyapunov--Razumikhin construction for the lifted history $(e_k,e_{k-1},\ldots,e_{k-2D})$.
The history is used in the proof; the online controller needs only the certified coefficients and does not store $2D+1$ dense matrices.

Define the aggregate stationary moments
\begin{equation}
K_q=\lambda_{\max}\!\left(\E_{\pi_q}[\bar H_k^\top\bar H_k]\right),
\quad
\Omega_q=\E_{\pi_q}\|\bar\xi_k\|^2,
\end{equation}
where $\bar H_k=q^{-1}\sum_iH_{i,k}$ and $\bar\xi_k=q^{-1}\sum_i\xi_{i,k}$.
Let $\delta=C_qr_q^b$ and $\mu_\delta=\mu-2L\delta$.
For the delay root-mean-square $\tau_{\rm rms}=(q^{-1}\sum_i\tau_i^2)^{1/2}$, define
\begin{align}
a_\delta(\eta)&=1-\eta\mu_\delta+\eta^2(2K_q+4L^2\delta),\nonumber\\
\beta_\delta(\eta)&=\frac{4\eta G^2\delta^2}{\mu_\delta}+\eta^2(2\Omega_q+4G^2\delta),\nonumber\\
h_\tau(\eta)&=2\eta^4L^4\tau_{\rm rms}^2,
\quad
g_\tau(\eta)=2\eta^4L^2G^2\tau_{\rm rms}^2.
\label{eq:affine-terms}
\end{align}
For positive delay, set $\lambda_\star=\sqrt{h_\tau/a_\delta}$ and
\begin{align}
c_{\rm aff}&=(1+\lambda_\star)a_\delta+(1+\lambda_\star^{-1})h_\tau,\nonumber\\
d_{\rm aff}&=(1+\lambda_\star)\beta_\delta+(1+\lambda_\star^{-1})g_\tau.
\label{eq:affine-cd}
\end{align}
The zero-delay definitions are the continuous limits $c_{\rm aff}=a_\delta$ and $d_{\rm aff}=\beta_\delta$.
The term $1-\eta\mu_\delta$ is the useful mean drift.
The $\eta^2K_q$ and $\eta^2\Omega_q$ terms price multiplicative and additive sampling noise, the $\delta$ terms price finite mixing, and the fourth-order terms price stale-state substitution.
Every term changes with at least one controlled coordinate of $a=(q,b,\eta)$.

\begin{theorem}[Affine delayed Markov Lyapunov bound]
Suppose Assumption~\ref{ass:catalogue} holds, $\mu_\delta>0$, $a_\delta(\eta)>0$, and $c_{\rm aff}(a)<1$.
Let $m=2D+1$ and $R_k=\max_{k-2D\le s\le k}\E\|e_s\|^2$.
Then, for every integer $n\ge0$,
\begin{equation}
R_{nm}\le c_{\rm aff}(a)^nR_0+\frac{d_{\rm aff}(a)}{1-c_{\rm aff}(a)}.
\label{eq:affine-bound}
\end{equation}
\label{thm:affine}
\end{theorem}
Theorem~\ref{thm:affine} turns the Lyapunov function $\|e\|^2$ and its delayed sliding envelope into a design objective.
Correlation enters $K_q$ and $\Omega_q$, temporal persistence enters $\delta$, and delay enters both the contraction and forcing terms.

\begin{corollary}[Finite-horizon return certificate]
Under Theorem~\ref{thm:affine}, let $n=\lfloor N_a(B,D)/(2D+1)\rfloor$ and let $\psi_\nu=\E_{s\sim\nu}\phi(s)$.
Then
\begin{equation}
\E\left|J_\nu(w_{N_a})-J_\nu(w^\star)\right|^2
\le \|\psi_\nu\|^2
\left[c_{\rm aff}^{n}R_0+\frac{d_{\rm aff}}{1-c_{\rm aff}}\right].
\label{eq:return-certificate}
\end{equation}
\label{cor:return}
\end{corollary}

The corollary connects the optimization variable to a reinforcement-learning quantity.
The controller does not maximize sampled return directly; it minimizes a certified value-estimation error for the fixed target policy.

The same certificate also tracks a critic target moved by a policy learner.
Let $w_r^\star$ be the projected fixed point during epoch $r$, lasting one replacement block $m=2D+1$, and let $Z_r=\max_{rm-2D\le s\le rm}\E\|w_s-w_r^\star\|^2$.

\begin{theorem}[Moving-policy critic tracking]
Suppose every epoch satisfies Assumption~\ref{ass:catalogue}, possibly with a different certified action $a_r$, and its coefficients obey $c_{\rm aff}(a_r)\le c<1$ and $d_{\rm aff}(a_r)\le d$.
If the policy-induced target moves only between epochs and
$\|w_{r+1}^\star-w_r^\star\|\le v$, then for every $\chi>0$ satisfying $\bar c=(1+\chi)c<1$,
\begin{align}
Z_n&\le \bar c^n Z_0+
\frac{(1+\chi)d+(1+\chi^{-1})v^2}{1-\bar c},\label{eq:tracking-bound}\\
\E|\widehat A_w-A^\star|^2
&\le (1+\gamma)^2\Phi^2 Z_n,\quad \|\phi(s)\|\le\Phi.
\label{eq:advantage-transfer}
\end{align}
\label{thm:tracking}
\end{theorem}

The bound exposes a moving-target phase: policy improvement increases $v$, while the resource action determines $(c,d)$.
Recomputing the joint score at actor-epoch boundaries therefore maintains a certified critic lag in centralized-training, decentralized-execution policy learning.

\subsection{Joint action selection}
The certificate score for action $a$ is
\begin{equation}
\mathcal{U}_j(a;B,D)=R_\star(a)+c_{\rm aff}(a)^{\lfloor N_a(B,D)/m\rfloor}\left(R_0-R_\star(a)\right)_+,
\label{eq:certificate-score}
\end{equation}
where $R_\star(a)=d_{\rm aff}(a)/(1-c_{\rm aff}(a))$.
The certified oracle for a known dependence instance is
\begin{equation}
a_j^\star(B,D)\in\arg\min_{a\in\A}\mathcal{U}_j(a;B,D).
\label{eq:catalogue-oracle}
\end{equation}
For every pair $(q,b)$, define the certified scalar domain
\begin{multline}
{\cal E}_{q,b}=\{\eta\in[\eta_{\min},\eta_{\max}]:
\mu_\delta>0,\\
a_\delta(\eta)>0,\ c_{\rm aff}(q,b,\eta)<1\}.
\label{eq:certified-step-domain}
\end{multline}
The controller solves
\begin{align}
\eta_{q,b}^\star\in\arg\min_{\eta\in{\cal E}_{q,b}}
&\mathcal U_j(q,b,\eta;B,D),\nonumber\\
(q^\star,b^\star)&\in\arg\min_{q,b}
\mathcal U_j(q,b,\eta_{q,b}^\star;B,D).
\label{eq:mixed-controller}
\end{align}
The first problem is a bounded scalar search.
The second is exact enumeration of physically discrete actor counts and clocks, not an optimization heuristic.

\begin{proposition}[Existence and computation]
If $K_q$, $\Omega_q$, and the mixing certificates are finite, then $\mathcal U_j$ is continuous on every connected component of ${\cal E}_{q,b}$ and attains a minimum on its closure after excluding points with $c_{\rm aff}\ge1$.
With $N_q=|{\cal Q}|$, $N_b=|{\cal B}|$, and $L_\eta$ scalar evaluations per search, the certificate cost is $O(N_qN_bL_\eta)$ arithmetic after the moment table is cached.
For rank-one temporal-difference samples, updating that table costs $O(qd)$ arithmetic and $O(d)$ working memory per retained block.
\label{prop:computation}
\end{proposition}

No Hessian inversion, covariance-matrix inversion, or preconditioner is required.
The dependence on $q$ is the unavoidable cost of aggregating the selected actor updates.

\subsection{Why minimizing one-step drift is insufficient}
A purely myopic drift rule would minimize $c_{\rm aff}(a)R_k+d_{\rm aff}(a)$.
Such a rule can prefer a large $q$ or $b$ even when its resource cost leaves too few future contractions.
Equation~\eqref{eq:certificate-score} instead composes the drift over the action-dependent horizon in \eqref{eq:usable-horizon}.
This changes the role of the Lyapunov function from a stability test to a finite-budget control objective.

The transient and noise-floor phases are also explicit.
When $R_0\gg R_\star(a)$, the exponential term dominates and a larger stable step can be valuable.
Near the floor, the residual term dominates and lower-correlation participation or greater physical spacing can be preferable.
The controller balances these phases within one score rather than switching through a separately tuned rule.

\section{Learning-Aware Dependence Adaptation}
\subsection{Adaptive information lower bound}
When $\theta_j$ is unknown, the controller may choose a predictable sequence $A_k=(q_k,b_k,\eta_k)$ and stop at a budget-bounded stopping time $\tau$.
Rotating $X_k$ into its normalized agent mean $Y_k=q_k^{-1/2}\bm{1}^\top X_k$ and orthogonal contrasts shows that only $Y_k$ contributes to the likelihood ratio.
Under hypothesis $j$, a scalar Kalman filter gives the predictive mean $m^-_{j,k}$ and variance $P^-_{j,k}$, with innovation variance $V_{j,k}=1+q_kP^-_{j,k}$.

Let $Z_k=(m^-_{0,k},P^-_{0,k},m^-_{1,k},P^-_{1,k})$.
The conditional directional information is
\begin{equation}
\begin{aligned}
\mathcal{I}_{01}(Z_k,A_k)=\frac12\bigg[&\log\frac{V_{1,k}}{V_{0,k}}-1\\
&+\frac{V_{0,k}+q_k(m^-_{0,k}-m^-_{1,k})^2}{V_{1,k}}\bigg].
\end{aligned}
\label{eq:conditional-kl}
\end{equation}

The dependence of information on the resource action is already visible under a fixed probe.
Let $T_n(r)$ be the $n\times n$ Toeplitz matrix with entries $[T_n(r)]_{uv}=r^{|u-v|}$, and let $\zeta_1,\ldots,\zeta_n$ be its eigenvalues.

\begin{proposition}[Exact fixed-probe information]
For a fixed probe $(q,b,n)$ with $r=\lambda^b$, the directional divergence is
\begin{equation}
\KL(P_0^{q,b,n}\|P_1^{q,b,n})
=\frac12\sum_{\ell=1}^n
\left[x_\ell-1-\log x_\ell\right],
\label{eq:fixed-probe-kl}
\end{equation}
where
\begin{equation}
x_\ell=\frac{1+q\theta_0\zeta_\ell}
{1+q\theta_1\zeta_\ell}.
\label{eq:fixed-probe-ratio}
\end{equation}
The Bhattacharyya exponent used by the upper rule is
\begin{equation}
I_B(q,b,n)=\frac12\sum_{\ell=1}^n
\log\frac{1+q(\theta_0+\theta_1)\zeta_\ell/2}
{\sqrt{(1+q\theta_0\zeta_\ell)(1+q\theta_1\zeta_\ell)}}.
\label{eq:bhattacharyya}
\end{equation}
\label{prop:fixed-information}
\end{proposition}

Equations \eqref{eq:fixed-probe-kl}--\eqref{eq:bhattacharyya} retain participation, spacing, persistence, probe length, and both dependence hypotheses.
They also show why many highly correlated actors need not provide proportional identification information: $q$ scales a common-mode eigenvalue rather than creating $q$ independent temporal records.

\begin{theorem}[Stopped adaptive information identity]
If the action kernel is predictable and common to both hypotheses and $\tau$ is bounded by \eqref{eq:budgets}, then
\begin{equation}
\KL(P_0^\pi|\F_\tau\,\|\,P_1^\pi|\F_\tau)=\E_0^\pi\sum_{k=1}^{\tau}\mathcal{I}_{01}(Z_k,A_k).
\label{eq:adaptive-kl}
\end{equation}
The reverse identity holds after exchanging the hypotheses.
\label{thm:adaptive-kl}
\end{theorem}
The realized action probabilities cancel in the likelihood ratio, while selection affects the occupation distribution of $(Z_k,A_k)$.
Consequently, any rule whose two directional decision errors are at most $\delta<1/2$ must satisfy
\begin{equation}
\E_j^\pi\sum_{k\le\tau}\mathcal{I}_{j,1-j}(Z_k,A_k)\ge \operatorname{kl}(1-\delta,\delta).
\label{eq:info-constraint}
\end{equation}
Together with \eqref{eq:budgets}, this gives an occupation-measure lower bound that prices participation, spacing, stopping, and delay without reducing the adaptive Markov experiment to independent action counts.

For a measurable belief-state set $G$, define the stopped occupation measure
\begin{equation}
\nu_j(G,a)=\E_j^\pi\sum_{k\le\tau}
\mathbf 1\{Z_k\in G,A_k=a\}.
\label{eq:occupation-measure}
\end{equation}
Let $\Delta_j(z,a)$ denote the certified one-step learning opportunity lost by using action $a$ for identification instead of the known-instance learning action.

\begin{proposition}[Adaptive opportunity lower bound]
Every predictable, budget-feasible, $\delta$-correct identification policy has expected opportunity cost at least
\begin{equation}
\inf_{\nu\in{\cal M}_j}
\int\Delta_j(z,a)\nu(dz,da),
\label{eq:occupation-lower-bound}
\end{equation}
where ${\cal M}_j$ contains the controlled Kalman-flow constraints, both resource constraints in \eqref{eq:budgets}, and the directional information constraint
\begin{equation}
\int\mathcal I_{j,1-j}(z,a)\nu(dz,da)
\ge\operatorname{kl}(1-\delta,\delta).
\label{eq:occupation-information}
\end{equation}
\label{prop:occupation}
\end{proposition}

The belief-state flow matters because an early choice of $(q,b)$ changes the predictive variance of later observations.
Replacing \eqref{eq:occupation-lower-bound} by independent action counts would discard this selection effect and can understate the required sensing cost.

\subsection{Learning-aware upper rule}
A fixed probe design $d=(q_d,b_d,n_d)$ consumes $C_d^{\rm msg}=n_d(h+q_d)$ and $C_d^{\rm env}=n_db_d$.
Its length is chosen so the exact Bhattacharyya exponent is at least $\log(1/\delta)$, which bounds each directional error by $\delta$.
Let $B_d^D$ be the remaining budget after the probe and terminal delay.

The probe is separated from the learning samples and is fully charged.
Separation prevents a data-dependent learning gradient from being reused as if it were an independent correlation certificate.
Full charging means that its $q_dn_d$ actor messages, $b_dn_d$ environment-clock units, and terminal delay are removed before any post-probe certificate is evaluated.

For instance $j$, define the full-budget all-agent certificate $\overline R_j^{\rm all}(B,D)$, the post-probe certificate optimum $\overline R_j^\star(B_d^D,D)$, and
\begin{align}
C_j^{\rm probe}(d)&=\overline R_j^{\rm all}(B_d,0)-\overline R_j^{\rm all}(B,0),\nonumber\\
C_j^D(d)&=\overline R_j^{\rm all}(B_d^D,D)-\overline R_j^{\rm all}(B_d,0),\nonumber\\
G_j(d)&=\overline R_j^{\rm all}(B_d^D,D)-\overline R_j^\star(B_d^D,D),\nonumber\\
W_j(d)&=\max_{\ell\ne j}\left[\overline R_j(a_\ell^\star;B_d^D,D)-\overline R_j^\star(B_d^D,D)\right]_+.
\label{eq:value-decomposition}
\end{align}
The low-instance safety slack is
\begin{equation}
\epsilon_{\rm safe}(d)=\left[C_0^{\rm probe}(d)+C_0^D(d)-G_0(d)+\delta W_0(d)\right]_+.
\label{eq:safety}
\end{equation}

The learning-aware controller probes only when
\begin{equation}
C_1^{\rm probe}(d)+C_1^D(d)+\delta W_1(d)<G_1(d)
\quad\text{and}\quad
\epsilon_{\rm safe}(d)\le\bar\epsilon.
\label{eq:admission}
\end{equation}
Otherwise it executes the all-agent fallback without spending a probe.
After an admitted probe, it commits to the certified catalogue minimizer for the identified instance.

The all-agent action is a transparent reference policy rather than an uncharged oracle.
Any other publicly specified certified fallback can replace it by substituting its risk in \eqref{eq:value-decomposition}; the proof is unchanged.

\begin{theorem}[Main finite-budget learning-value guarantee]
Suppose Assumption~\ref{ass:catalogue} holds for both instances, the probe is feasible under \eqref{eq:budgets}, and its directional errors are bounded by $\delta$.
Every design admitted by \eqref{eq:admission} satisfies
\begin{equation}
\E_j\|e_{\rm LA}\|^2\le \overline R_j^\star(B_d^D,D)+\delta W_j(d),
\label{eq:main-risk}
\end{equation}
has strictly smaller certified high-instance risk than the full-budget all-agent action, and has low-instance risk excess at most $\bar\epsilon$.
If no design is feasible or admitted, the controller returns the all-agent action and incurs no probing cost.
\label{thm:main}
\end{theorem}
The theorem aligns the decision time with the risk being certified: the action is chosen before its learning block, and every cost in \eqref{eq:main-risk} is charged before evaluating the remaining horizon.
It also separates three claims that are often conflated.
The likelihood exponent certifies identification, the admission inequality certifies positive high-instance learning value, and $\epsilon_{\rm safe}$ certifies the declared low-instance tolerance.
None follows from the other two alone.

\subsection{Finite-budget phase theorem}
Let budgets grow on a public ray $(B_{\rm msg}(s),B_{\rm env}(s))=(\lfloor s\beta_m\rfloor,\lfloor s\beta_e\rfloor)$.
Consider a compact class with $0<\theta_-\le\theta_j\le\theta_+$, $|\theta_1-\theta_0|\ge\Delta_{\min}>0$, $\lambda\le1-\gamma$, bounded delay, finite ${\cal Q}$ and ${\cal B}$, and a compact certified step domain.
Assume one fixed probe has uniformly positive directional Bhattacharyya information per retained observation, the catalogue risks obey the uniform expansion $\overline R_j(a;s)=K_j(a)/s+O(s^{-2})$, and the high-instance coefficient gap between the all-agent action and the oracle is at least $g_{\min}>0$.
Finally, assume the catalogue's wrong-commit coefficient is uniformly bounded and the declared error ceiling $\delta_0$ leaves at least half of $g_{\min}$ after charging that term.
Let $B_N$ be the smallest scale satisfying the two directional information constraints and $B_S$ the smallest scale at which an admitted probe exists.

\begin{theorem}[Necessary--sufficient threshold sandwich]
For every fixed admissible safety target and $0<\delta\le\delta_0<1/2$, there are class constants $C_{\mathcal K},C'_{\mathcal K}<\infty$ such that
\begin{equation}
B_N\le B_S\le C_{\mathcal K}B_N+C'_{\mathcal K}(B_{\rm oracle}+B_\epsilon+1).
\label{eq:threshold-sandwich}
\end{equation}
Below $B_N$, $\delta$-correct identification is impossible, while above $B_S$ the learning-aware controller achieves the guarantees of Theorem~\ref{thm:main}.
\label{thm:sandwich}
\end{theorem}
Theorem~\ref{thm:sandwich} connects information and learning rather than treating identification as the final objective.
The interval $[B_N,B_S)$ is the identifiable-but-not-yet-valuable phase in which information-only probing spends resources that the remaining learner cannot amortize.

The separated-class conditions are substantive.
If $\lambda$ can approach one without an independent mixing certificate, finite samples cannot uniformly distinguish a persistent common factor from its initial condition.
If $|\theta_1-\theta_0|$ can vanish, the required identification budget diverges.
The positive theorem therefore states precisely the regime in which learning-aware adaptation is possible rather than hiding these quantities inside an unspecified mixing constant.

\section{Implementation and SDDE Interpretation}
\subsection{End-to-end controller}
Algorithm~\ref{alg:controller} shows the complete decision path.
The public catalogue and confidence level are fixed before observations are drawn.
Moment and mixing certificates may come from a system specification or a disjoint calibration stream; the likelihood probe is used only to select between registered dependence instances.

\begin{figure}[t]
\centering
\begin{minipage}{0.97\columnwidth}
\begin{algorithmic}[1]
\STATE \textbf{Input:} budgets $B$, delay profile, action sets ${\cal Q},{\cal B}$, step interval, risk allowance $\bar\epsilon$
\FOR{each dependence instance $j$ and pair $(q,b)$}
\STATE form $\delta$, $K_q$, $\Omega_q$, and the certified set ${\cal E}_{q,b}$
\STATE solve the scalar problem in \eqref{eq:mixed-controller}
\ENDFOR
\STATE obtain the instance actions $a_j^\star$ and their finite-budget risks
\FOR{each feasible registered probe $d$}
\STATE charge its message, environment, and delay costs
\STATE evaluate $C_j^{\rm probe}$, $C_j^D$, $G_j$, $W_j$, and $\epsilon_{\rm safe}$
\ENDFOR
\IF{some probe satisfies \eqref{eq:admission}}
\STATE choose the admitted probe with smallest certified post-probe risk
\STATE observe the charged probe, run the likelihood test, and commit to $a_{\widehat j}^\star$
\ELSE
\STATE execute the certified fallback without probing
\ENDIF
\STATE \textbf{Output:} value parameter and complete resource ledger
\end{algorithmic}
\end{minipage}
\caption{Learning-aware Lyapunov resource control.}
\label{alg:controller}
\end{figure}

The computation separates statistics from control.
The spatial moment table stores scalar upper bounds on $K_q$ and $\Omega_q$ for each feasible participation.
The temporal table stores $(C_q,r_q)$ and evaluates $C_qr_q^b$ for each spacing.
Once these tables are available, changing a budget or a delay profile requires only scalar arithmetic and bounded search.

\subsection{Predictable confidence interfaces}
The theorem requires upper certificates, not plug-in point estimates treated as truth.
For a calibration block ending before action selection, let $\widehat K_q$, $\widehat\Omega_q$, and $\widehat r_q$ be estimators and let $u_K,u_\Omega,u_r$ be simultaneous error radii.
The controller uses
\begin{equation}
K_q^+=\widehat K_q+u_K,
\quad
\Omega_q^+=\widehat\Omega_q+u_\Omega,
\quad
r_q^+=\min\{1,\widehat r_q+u_r\}.
\label{eq:upper-interfaces}
\end{equation}
On the joint coverage event, substituting these upper quantities preserves every certificate inequality.
If $r_q^+=1$, or if the resulting $\mu_\delta$ or step domain is invalid, that action is removed rather than optimistically extrapolated.
This predictable interface prevents current-block residuals from being counted repeatedly as independent evidence.

The confidence mechanism is deliberately modular.
Finite-state chains can use spectral or regeneration certificates, while geometrically ergodic continuous-state chains can use an externally supplied mixing upper bound.
The controller's guarantee is conditional on the declared simultaneous coverage event, and its failure probability adds directly to the risk statement by total probability.

\subsection{Lyapunov design through a controlled SDDE}
The controlled stochastic delay differential equation associated with one fixed resource action is
\begin{equation}
dE(t)=-A_qE(t-\tau(t))dt
+\sqrt{\eta}\,\Sigma_q^{1/2}(E_t)dW(t).
\label{eq:controlled-sdde}
\end{equation}
Here $\Sigma_q(E_t)$ retains both additive innovation covariance and the state-dependent quadratic variation of random temporal-difference Jacobians.
For $V(E)=\|E\|^2$, It\^o's formula gives the infinitesimal drift
\begin{equation}
{\cal L}_aV(E_t)
=-2E(t)^\top A_qE(t-\tau(t))
+\eta\tr\Sigma_q(E_t).
\label{eq:ito-drift}
\end{equation}
Participation controls the spatial covariance in $\Sigma_q$, spacing controls the finite-mixing remainder in both terms, and $\eta$ controls their relative scale.
This calculation motivates optimizing a drift-based resource action rather than attaching a stability proof to a variance heuristic.

Equation~\eqref{eq:controlled-sdde} is not used to replace the implementable recursion with a continuous approximation.
Bounding the delayed cross term in \eqref{eq:ito-drift} produces the same contraction--forcing decomposition as \eqref{eq:affine-terms}, while Theorem~\ref{thm:affine} retains finite steps, integer delays, and exact budget rounding.
The SDDE therefore supplies the design geometry; the discrete Lyapunov theorem supplies the finite-time guarantee used by the algorithm.

\subsection{Complexity and deployment}
For $N_qN_b$ discrete pairs and $L_\eta$ scalar evaluations, action construction costs $O(N_qN_bL_\eta)$ after calibration.
The likelihood recursion is scalar because the orthogonal spatial contrasts cancel under the two common-factor hypotheses.
Online aggregation of rank-one temporal-difference samples costs $O(qd)$ arithmetic and $O(d)$ extra memory.
The controller is therefore low complexity relative to the $O(qd)$ update it regulates.

The method concerns centralized training of a common fixed-policy value function.
After training, policy execution is decentralized and needs no communication because the target policy is already fixed.
This separation makes the resource controller a training-layer component that leaves the deployed policy and its execution interface unchanged.

\section{Experiments}
\subsection{Protocol and evaluation questions}
The experiments test five predictions in increasing order of algorithmic scope.
The first asks whether cross-agent correlation produces the saturation in \eqref{eq:speedup} and moves the resource-optimal participation level.
The second asks whether Theorem~\ref{thm:affine} is finite, empirically calibrated, and responsive to correlation, mixing, and delay when it jointly selects $(q,b,\eta)$.
The third asks whether the learning-value gate in \eqref{eq:admission} improves risk over a controller that probes as soon as identification becomes reliable.
The fourth asks whether joint control improves the entire resource-indexed convergence trajectory and the induced discounted-return estimate relative to a strong fixed-participation policy.
The fifth asks whether the same observable dependence decision transfers to centralized-training--decentralized-execution policy learning and improves team return under equal resources.

All studies use paired common random numbers across policies, charge every message and environment-clock unit, retain every registered cell, and use independent same-seed reproductions or disjoint confirmation seeds.
The Markov temporal-difference task has seven states on a ring, a bounded sinusoidal reward, four stationary-distribution-orthonormal features, and an exact projected fixed point.
Cross-agent dependence is induced by shared and private Markov sources while preserving every agent's marginal transition law.
The policy-learning study uses the discrete PettingZoo multi-agent particle environment \texttt{simple\_spread\_v2} and multi-agent proximal policy optimization (MAPPO) with parameter-shared actors and a centralized critic~\cite{terry2021pettingzoo,yu2022mappo}.

Parameter risk is $\|w-w^\star\|^2$ normalized by its value before learning.
Return-estimation risk is $|V_w(s_0)-V_{w^\star}(s_0)|^2$ under the registered initial state and is normalized in the same way.
Area under the convergence curve is computed against charged resource fraction, so a policy cannot appear faster by ignoring the samples or messages used to create its updates.
Bootstrap comparisons resample paired seeds and report percentile intervals for the ratio of geometric-mean risks.

\subsection{Correlation-limited participation}
EXP-007A evaluates $q\in\{1,2,4,8,16,32\}$, six cross-agent correlation levels, three delays, three message-equivalent budgets, thirteen step sizes, and 32 paired seeds.
All 134,784 rows are finite and budget-valid.
At $q=32$, median effective participation is $30.996$ for independent trajectories and $1.111$ at correlation $0.9$.
At the largest registered budget and delay eight, the resource-optimal participation moves from $q=16$ under independence to $q=1$ under high sharing.
Using the endpoint action selected for the opposite correlation regime increases error by factors of approximately $4.29$ and $3.13$, respectively.
These results isolate the statistical and resource mechanism that motivates controlling participation.
They also rule out an update-count interpretation of the gain: every endpoint is compared after applying the same registered message-equivalent budget.

\subsection{Finite-time certificate and joint actions}
EXP-010B evaluates twelve $(\text{persistence},\rho,D)$ scenarios with 32 new paired seeds and 1,152 policy runs.
The joint controller searches a stability-screened catalogue of $q\in\{1,2,4,8,16,32\}$, charged spacings, and scalar steps.
All selected actions satisfy $0<c_{\rm aff}<1$, all 1,152 runs are finite and budget-valid, and the largest algebraic discrepancy between the two equivalent drift expressions is $2.22\times10^{-16}$.

The finite-time bound beats the no-update risk in all twelve scenarios (12/12).
Every one-sided 99 percent bootstrap upper mean lies below its corresponding bound, with a maximum empirical-upper-bound to theorem-bound ratio of $0.305$.
The median theorem-bound to empirical-mean ratio is $20.77$ and the maximum is $228.98$, so the certificate is conservative but finite and action-informative.

Fig.~\ref{fig:joint-actions} shows that the selected participation and spacing respond in the directions predicted by the theory.
The controller selects an average participation of $12.67$ for independent sources and $q=1$ in every high-sharing scenario.
Delay reduces the selected independent-source participation to $q=4$, while raising temporal persistence from zero to $0.98$ increases the median paid spacing by a factor of $18.49$.
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/exp010b_joint_actions.pdf}
\caption{Joint actions selected by the finite-time Lyapunov certificate in EXP-010B.
The left panel reports participation and the right panel reports Markov spacing.}
\label{fig:joint-actions}
\end{figure}

\subsection{Learning-value adaptation}
EXP-016B uses 96 finite-threshold scenario families, two model layers, eight policies, and 192 independent formal seeds.
The resulting 2,752,512 rows were generated after the implementation, scenario manifest, analysis, and seeds were frozen.
The primary estimand is the paired risk difference between probing at $B_{\rm id}$ and applying the learning-value gate in the separation phase.

The learning-aware controller reduces Layer-A risk by $54.43$ percent and affine delayed temporal-difference risk by $10.85$ percent.
The simultaneous one-sided lower bounds on the absolute paired improvements are $0.1612$ and $0.0577$, respectively.
Directional practical improvement occurs in 77 of 96 scenario families.
All 96 theorem-facing safety scenarios satisfy $S_{\rm mean}\le\epsilon_{\rm safe}$, and every trajectory obeys both resource budgets.

Fig.~\ref{fig:risk-reduction} separates the conditions under which the value gate is most consequential.
The relative risk reductions are $49.12$ percent with active delay, $77.69$ percent when messages bind, and $43.13$ percent when environment transitions bind.
The result supports the mechanism in \eqref{eq:value-decomposition}: a reliable probe is most costly when it consumes the resource that limits the remaining learner.
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/exp016b_risk_reduction.pdf}
\caption{Registered relative risk reductions of the learning-aware rule over information-only probing in EXP-016B.
The dashed line is the preregistered three-percent practical-effect threshold.}
\label{fig:risk-reduction}
\end{figure}

\subsection{Convergence and return-estimation curves}
TSP-CURVE-001 tests the complete joint controller on the seven-state policy-evaluation problem over a budget of 128,000 resource units.
The registered grid crosses persistence $\{0,0.9,0.98\}$, correlation $\{0,0.9\}$, and delay $\{0,8\}$, producing twelve cells and 41 charged-resource checkpoints per trajectory.
The fixed policies use $q\in\{1,4,16,32\}$; within every cell, each fixed $q$ receives its own certified spacing and step, so the comparison isolates the value of jointly changing participation rather than handicapping its optimizer.

The strong fixed comparator is selected using 16 development seeds and then frozen.
It is $q=4$, which has geometric normalized parameter-error AUC $0.2441$ on the development population, compared with $0.3342$, $0.4649$, and $0.4851$ for $q=1,16,32$.
The final comparison uses 64 disjoint confirmation seeds and 157,440 finite checkpoint rows.

Relative to fixed $q=4$, the joint certificate has parameter-error AUC ratio $0.9009$ with paired-bootstrap 95 percent interval $[0.8914,0.9102]$.
Its discounted-return-estimation AUC ratio is $0.8868$ with interval $[0.8678,0.9072]$.
These correspond to reductions of $9.91$ and $11.32$ percent.
Terminal parameter and return-estimation errors decrease by $26.48$ and $21.67$ percent, respectively.
Both AUC metrics improve in nine of twelve cells and tie in the remaining three; no cell is worse.

Fig.~\ref{fig:convergence-curves} displays the representative persistence-$0.9$, zero-delay slice.
Under independent actor innovations, the joint rule follows the broad-participation action and contracts rapidly.
Under correlation $0.9$, it follows the low-participation action and avoids the common-noise floor.
The figure therefore visualizes the same mechanism predicted by \eqref{eq:speedup} and selected by \eqref{eq:mixed-controller}, rather than a post hoc schedule.
\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{figures/convergence_curves.pdf}
\caption{Held-out resource-indexed convergence in TSP-CURVE-001 for persistence $0.9$ and zero delay (64 confirmation seeds; bands are 95 percent confidence intervals).
The first two panels show normalized parameter error; the last two show normalized squared error of the fixed-policy discounted-return estimate.
The joint Lyapunov certificate changes participation with the dependence regime and improves both learning curves relative to the development-selected strong fixed $q=4$ comparator.}
\label{fig:convergence-curves}
\end{figure*}

\subsection{Transfer to cooperative policy learning}
TSP-MARL-CONF-001 is a separately preregistered MAPPO experiment with eight new training seeds and eight disjoint probe seeds.
Each method receives a message budget of five million and an environment budget of one million ticks; the controller first charges 128 non-learning critic-residual probe blocks and then commits to $q=1$ or $q=8$ without observing evaluation return.
The 48 runs cross independent and shared rollout innovations with fixed $q=1$, fixed $q=8$, and the Lyapunov probe-then-commit controller.

The controller selects $q=8$ in all eight independent-stream runs and $q=1$ in all eight shared-stream runs.
Relative to fixed $q=8$, its mean team-return AUC change is $-0.357$ percent for independent streams and $+4.996$ percent for shared streams; the corresponding one-sided 95 percent lower bounds are $-0.393$ and $+2.189$ percent.
The equal-weight mixture gain is $2.319$ percent with a $0.917$ percent lower bound, while the shared-stream comparison with regime-matched fixed $q=1$ remains within the preregistered two-percent noninferiority margin (lower bound $-1.345$ percent).

Fig.~\ref{fig:marl-return} shows the unsmoothed deterministic team-return curves against the maximum charged dual-budget fraction.
The probe consumes at most $0.768$ percent of the message budget, and scalar selection overhead is below $10^{-6}$ of training time.
This experiment evaluates transfer of the observable dependence-selection mechanism through a frozen empirical correlation proxy; the theorem-facing confidence guarantee remains the affine delayed Markov result.
\begin{figure*}[t]
\centering
\includegraphics[width=0.72\textwidth]{figures/marl_probe_commit_return_curves_compact.pdf}
\caption{Preregistered MAPPO team-return learning curves in TSP-MARL-CONF-001 (eight held-out seeds per dependence regime; bands show 95 percent confidence intervals).
The fully charged return-free controller commits to fixed $q=8$ under independent rollout innovations and fixed $q=1$ under shared innovations, tracking the regime-matched fixed action while improving over globally fixed broad participation.}
\label{fig:marl-return}
\end{figure*}

\subsection{Ablations, complexity, and reproducibility}
For a finite catalogue, action evaluation is linear in the number of candidate triples.
The exact likelihood update is scalar after the spatial reduction, and the affine certificate uses cached scalar moments and a one-dimensional step search.
When temporal-difference Jacobians are rank one, online moment probes require $O(qd)$ arithmetic and $O(d)$ memory, matching the order of aggregating $q$ agent updates.

The four controlled Markov studies ran on CPUs and reproduced their registered primary artifacts byte for byte.
For TSP-CURVE-001, the action set, metrics, 16 development seeds, 64 confirmation seeds, and bootstrap rule were fixed before confirmation; raw hashes, aggregate curves, and analysis code accompany the repository.
TSP-MARL-CONF-001 ran as six immutable eight-cell arrays on NVIDIA A30 GPUs; all 48 runs were finite, exactly budgeted, and generated byte-identical gate, curve, and figure artifacts under an independent analysis replay.

The ablations isolate all three coordinates: fixing $q$ removes the correlation response in Fig.~\ref{fig:convergence-curves}, fixing $b$ removes the $18.49$-fold persistence response, and fixing $\eta$ ignores the delay-dependent domain in \eqref{eq:certified-step-domain}.
Their gaps distinguish the controller from an adaptive batch-size rule.

\section{Conclusion}
Multi-agent Markov policy evaluation requires a resource decision before the next update's value is known.
Our finite-budget Lyapunov certificate jointly chooses participation, physical spacing, and step size; a learning-aware gate measures dependence only when the remaining learner can use it.
The theory identifies a spatial speedup ceiling, proves delayed Markov convergence and moving-policy critic tracking, lower-bounds adaptive identification cost, and separates the information and learning-value budget thresholds.
Fully charged experiments connect action selection to parameter error, return estimation, and cooperative team-return curves.
The low-complexity controller uses Lyapunov drift as a design tool and identifies when correlated parallelism is valuable.
Routine resource-geometry derivations are provided in the supplementary material.

\appendices
\input{appendices}
\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}
